{
  "name": "rocket-shooter-repo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "cd web-client && npm run serve",
    "build": "tsc && cd web-client && npm run build",
    "lint": "cd web-client && vue-cli-service lint",
    "publish-web": "tsc && cd web-client && npm run build && git add . && git commit && git push"
  }
}
